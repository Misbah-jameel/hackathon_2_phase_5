# Database
# SQLite for local development (default):
DATABASE_URL=sqlite:///./todoapp.db
# PostgreSQL/Neon for production:
# DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# JWT Configuration
# IMPORTANT: Generate a secure secret for production!
# Run this command to generate a secure secret:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# Then replace the value below with your generated secret.
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=24

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Anthropic AI Configuration (optional - enables AI-powered chatbot)
# Get your API key at https://console.anthropic.com/
# Leave empty to use regex-based intent detection (fallback mode)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-haiku-20240307
